$kind: http-request
name: Get Profile
description: "Retorna o perfil do estudante autenticado (User + Address? + StudentCourse ativo)."
method: GET
url: "{{BASE_URL}}/student/profile"
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: "{{TOKEN_STUDENT}}"
