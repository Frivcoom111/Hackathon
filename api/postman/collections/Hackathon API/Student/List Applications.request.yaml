$kind: http-request
description: Lista as candidaturas do estudante autenticado (paginado).
url: "{{BASE_URL}}/student/applications?page=1&limit=10"
method: GET
queryParams:
  page: "1"
  limit: "10"
auth:
  type: bearer
  credentials:
    token: "{{TOKEN_STUDENT}}"
order: 5000
