$kind: http-request
name: List Members
description: "Lista os membros da empresa. Somente ADMIN da empresa."
method: GET
url: "{{BASE_URL}}/company/members"
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: "{{TOKEN_COMPANY}}"
