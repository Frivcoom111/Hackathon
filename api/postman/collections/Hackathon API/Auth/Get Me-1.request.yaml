$kind: http-request
name: Get Me
description: "FASE 1 — Rota protegida. Retorna os dados do usuário autenticado a partir do JWT enviado no header Authorization."
method: GET
url: "{{BASE_URL}}/auth/me"
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: "{{TOKEN_USER}}"
