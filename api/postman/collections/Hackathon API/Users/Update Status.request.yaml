$kind: http-request
url: "{{BASE_URL}}/users/:id/status"
method: PATCH
headers:
  Content-Type: application/json
pathVariables:
  id: ""
body:
  type: json
  content: |-
    {
      "isActive": true
    }
auth:
  type: bearer
  credentials:
    token: "{{TOKEN_ADMIN}}"
order: 8000
